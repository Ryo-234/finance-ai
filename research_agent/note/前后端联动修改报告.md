# 前后端联动修改报告

## 修改日期
2026-05-28

## 问题描述
research_agent 前后端分开开发，接口尚未对接。

## 已完成的修改

### 1. 后端路由前缀修复

**问题**：`app/api/app.py` 中给 `threads.router` 添加了额外的 `prefix="/api/threads"`，但 `threads.router` 本身已经定义了 `prefix="/api/threads"`，导致路由变成 `/api/threads/api/threads/` 而不是 `/api/threads/`。

**修改文件**：`api/app.py`

**修改内容**：
```python
# 修改前
app.include_router(threads.router, prefix="/api/threads", tags=["threads"])
app.include_router(memory.router, prefix="/api/memory", tags=["memory"])
app.include_router(models.router, prefix="/api/models", tags=["models"])

# 修改后
app.include_router(threads.router, tags=["threads"])
app.include_router(memory.router, tags=["memory"])
app.include_router(models.router, tags=["models"])
```

**原因**：`threads.router`、`memory.router`、`models.router` 在定义时已经包含了 `prefix="/api/..."`，所以在 `app.py` 中不应该再添加前缀。

### 2. Git 分支保存

**分支**：`research-agent-standalone`

**提交历史**：
- `a05c880` - 初始状态保存（包含所有初始文件）
- `9ba9d24` - 核心文件更新

**回退命令**：`git reset --hard <commit-id>`

## 验证结果

### 后端 API 测试

```bash
# 健康检查
curl http://127.0.0.1:8001/api/health
# 返回：{"status":"healthy","service":"research-agent-gateway","version":"1.0.0"}

# 创建线程
curl -X POST http://127.0.0.1:8001/api/threads/ -H "Content-Type: application/json" -d '{"channel":"api","chat_id":"test"}'
# 返回：{"thread_id":"xxx","channel":"api","chat_id":"anonymous","created_at":...,"status":"idle","message_count":0}

# 聊天（非流式）
curl -X POST http://127.0.0.1:8001/api/chat/ -H "Content-Type: application/json" -d '{"message":"你好","thread_id":"xxx"}'
# 返回：{"answer":"...","thread_id":"xxx","sources":[],"tasks":[],"error":null}

# 聊天（流式）
curl -X POST http://127.0.0.1:8001/api/chat/stream -H "Content-Type: application/json" -d '{"message":"你好","thread_id":"xxx","stream":true}'
# 返回 SSE 格式的流式响应
```

## 当前状态

### 路由结构（已验证）
```
/api/health                         - 健康检查
/api/threads/                      - 创建线程
/api/threads/{thread_id}           - 获取线程
/api/threads/{thread_id}/status   - 更新状态
/api/chat/                         - 聊天（非流式）
/api/chat/stream                   - 聊天（流式）
/api/chat/upload                  - 图片上传
/api/memory/                       - 记忆管理
/api/models/                       - 模型列表
/api/channels/                      - 渠道管理
```

### 前端页面结构
```
/              → LandingPage（落地页）
/chat          → HomePage（聊天 UI）
```

## 待验证项

1. 前端是否能正常启动并连接到后端
2. 前端 `useChat` hook 是否正确调用后端 API
3. SSE 流式响应是否能正确解析和显示

## 启动命令

### 后端
```bash
cd research_agent
python run_api.py --port 8001
```

### 前端
```bash
cd research_agent/frontend
pnpm dev
```

## 回退方法

如需回退到修改前的状态：
```bash
git reset --hard a05c880  # 回退到初始状态
git reset --hard 9ba9d24  # 回退到核心文件更新
```
